name: Deploy to EC2 instance 

on:
  push:
    branches:
      - main 
      - multiplayer-online

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository 
      uses: actions/checkout@v3 

    - name: Install SSH client 
      run: sudo apt-get install -y openssh-client 

    - name: test step
      run: echo "Testin in the job rn"